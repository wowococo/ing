{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileBasename}",
            "cwd": "${workspaceFolder}/src",
            "env": {
                "GO111MODULE": "on"
            },

        }
    ]
}